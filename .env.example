# Both are PUBLIC (the anon key is RLS-gated). Optional — vite.config.ts has the
# production defaults baked in. Override only to point the widget at another env.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
